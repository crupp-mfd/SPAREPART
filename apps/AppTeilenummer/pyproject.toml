[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "app-teilenummer"
version = "0.1.0"
description = "MFDApps Teilenummer API"
requires-python = ">=3.11"
