[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "app-wagensuche"
version = "0.1.0"
description = "MFDApps Wagensuche API"
requires-python = ">=3.11"
