[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "app-goldenview"
version = "0.1.0"
description = "MFDApps GoldenView API"
requires-python = ">=3.11"
