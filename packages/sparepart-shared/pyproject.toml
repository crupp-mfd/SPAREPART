[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sparepart-shared"
version = "0.1.0"
description = "Shared runtime helpers for split MFDApps services"
requires-python = ">=3.11"
